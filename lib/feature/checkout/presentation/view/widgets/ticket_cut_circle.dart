

import 'package:flutter/material.dart';

class TicketCutCircle extends StatelessWidget {
  const TicketCutCircle({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircleAvatar(
      radius: 20,
      backgroundColor: Colors.white,
    );
  }
}